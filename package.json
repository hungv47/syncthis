{
  "name": "syncthis",
  "version": "0.1.0",
  "description": "Install your AI tools once. Use them everywhere. MCP server config sync across Claude Code, Cursor, Codex, and Gemini CLI.",
  "type": "module",
  "bin": {
    "syncthis": "./bin/syncthis.ts"
  },
  "files": [
    "bin",
    "src"
  ],
  "scripts": {
    "test": "bun test",
    "build": "bun build ./bin/syncthis.ts --compile --outfile dist/syncthis"
  },
  "dependencies": {
    "smol-toml": "^1.3.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
